import React from 'react';

function VisualCanvas() {
  return (
    <div style={{ marginTop: '40px' }}>
      <h2>📍 Network Map (Visualization Coming Soon)</h2>
      <div style={{
        width: '100%',
        height: '300px',
        border: '2px dashed #ccc',
        borderRadius: '10px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#999'
      }}>
        Towers and Users will be shown here visually in future version
      </div>
    </div>
  );
}

export default VisualCanvas;
